spring:
  profiles:
    active: dev
  application:
    name: SPRING-CLOUD-WEB
eureka:
  instance:
    lease-expiration-duration-in-seconds: 3
    lease-renewal-interval-in-seconds: 1
server:
  port: 11001
